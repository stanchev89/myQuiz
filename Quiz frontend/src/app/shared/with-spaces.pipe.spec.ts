import { WithSpacesPipe } from './with-spaces.pipe';

describe('WithSpacesPipe', () => {
  it('create an instance', () => {
    const pipe = new WithSpacesPipe();
    expect(pipe).toBeTruthy();
  });
});
