import { FixHtmlSymbolsPipe } from './fix-html-symbols.pipe';

describe('FixHtmlSymbolsPipe', () => {
  it('create an instance', () => {
    const pipe = new FixHtmlSymbolsPipe();
    expect(pipe).toBeTruthy();
  });
});
