import { NumberRankingPipe } from './number-ranking.pipe';

describe('NumberRankingPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberRankingPipe();
    expect(pipe).toBeTruthy();
  });
});
