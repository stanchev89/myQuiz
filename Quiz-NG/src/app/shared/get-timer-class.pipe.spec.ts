import { GetTimerClassPipe } from './get-timer-class.pipe';

describe('GetTimerClassPipe', () => {
  it('create an instance', () => {
    const pipe = new GetTimerClassPipe();
    expect(pipe).toBeTruthy();
  });
});
